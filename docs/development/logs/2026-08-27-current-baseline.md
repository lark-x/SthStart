# 2026-08-27：当前 main 开发基线

状态：部分完成  
关联事项：DEV-001～DEV-009、FIX-001～FIX-005、DOC-001～DOC-002  
关联提交：`52bdf12`

## 本轮目标

- 为后续开发建立统一、可持续维护的项目状态与开发日志体系。
- 以当前 `main` 为事实基线，不完整回溯此前每次提交。
- 将开发缺口与延后验收分开，允许继续推进功能，同时保留明确的验收债务。

## 当前已经形成的能力

- SthStart 已具备门户、控制中心、公共服务、邻舍接入、创作笔记、叙事档案和角色库等应用基础。
- 公共服务已提供应用身份、公共 LLM、向量、角色模板、日志、Artifact 和 Generation 能力。
- Artifact 2.0 已建立中央媒体存储、引用、授权、流式访问、Range、固定、回收和磁盘保护骨架。
- Generation Core 已具备任务、工作流版本、调度、租约、重试、取消、恢复、事件和 SSE。
- Windows Worker 已具备鉴权、IP 白名单、磁盘保护、任务恢复、流式媒体输入和多种媒体输出清单。
- 创作中心已支持图片生成和媒体库，并建立视频生成模式、任务状态和播放器界面基础。
- H3 已有 T2V、I2V、FL2VA 类型、分配、状态检查和 Mock 生命周期基础。

## 重要判断

- 当前提交更适合定义为“媒体平台与 H3 工作流基础”，不代表真实 H3 已完整可用。
- Generation 已明显减少旧图片系统的重复路径，但执行和管理逻辑仍然集中，需要在继续增加引擎前收口。
- Worker 已出现通用媒体协议实现，但真实视频、音频、大文件和 Windows H3 环境仍缺完整验证。
- 验收类工作本阶段不阻塞继续开发，统一记录在 `DEFERRED_VALIDATION.md`，后续稳定化阶段集中处理。

## 已知限制与待办

- H3 就绪检查尚未严格验证具体模型、自定义节点和版本，见 FIX-001。
- 普通管理流程无法完整创建 Worker/H3 工作流，见 FIX-002。
- Generation 核心执行文件和管理服务仍过大，见 DEV-001～DEV-003。
- 正式 H3 工作流资产、系统资源诊断和队列管理仍待开发，见 DEV-004～DEV-006。
- 真实 H3、Worker 多媒体、视频后处理、浏览器和全平台 CI 验收已登记为 VAL-001～VAL-012。

## 基线验证记录

- 类型检查和构建已通过。
- Portal 单元测试与现有 Worker 测试通过。
- Service 测试大部分通过；两个邻舍运行测试受到本机已有进程占用固定端口影响，已登记 FIX-003。
- 本轮浏览器测试复用了非 E2E 配置的本地服务，结果不能作为代码验收结论。
- 尚无本基线对应的完整远端全平台 CI 结论。

## 文档影响

- 新增开发记录入口、当前状态、Backlog、延后验收、维护规范和日志模板。
- 根目录实施计划继续作为目标设计文档保留，不将其中的目标自动视为已完成能力。
