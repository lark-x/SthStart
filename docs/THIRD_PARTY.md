# 第三方代码与分发边界

## 邻舍.EXE

- 来源：`icecranberry/galgame-with-comfyUI`
- 接入方式：Git Submodule `upstream/linshe`
- 当前固定版本：v3.0.3（`db47dafbdacb3d877a3cfec30bdefe780f86a58d`）

SthStart 不复制、改写或重新授权邻舍源码。邻舍的代码、名称、素材及其他内容仍归原项目贡献者所有，并由原项目自身的授权条款约束。

在当前固定提交中没有发现 `LICENSE` 或 `COPYING` 文件。这不影响在本地通过 Submodule 进行技术集成，但若要在 GitHub Fork 之外重新分发、打包发布或用于商业用途，应先向原作者确认授权范围。后续同步到新版本时，需要重新检查原项目是否增加或变更许可证。

模型文件、ComfyUI 模型与用户生成内容不进入 SthStart Git 仓库；它们分别遵循各自来源的许可证与使用条款。
